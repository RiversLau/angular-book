ng-bind指令
    1、<p>{{greeting}}</p>与<p ng-bind="greeting"></p>等价
    2、为什么要使用一种方式替换另一种方式呢？
        因为在使用双花括号语法时，在Angular使用数据替换这些花括号之前，第一个被加载的页面，也就是应用的index.html，
    它未被渲染好的模版可能会被用户看到，而第二种方式不会出现此类问题。
